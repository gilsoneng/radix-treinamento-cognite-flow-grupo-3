// Registra os matchers do jest-dom (ex.: toBeInTheDocument) no `expect` do Vitest.
// Referenciado por `vitest.config.ts` (setupFiles). Sem este arquivo, nenhum teste roda.
import '@testing-library/jest-dom/vitest';

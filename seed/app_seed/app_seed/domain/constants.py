"""Constantes de domínio do seed (valores de propriedades, não identificadores)."""

from __future__ import annotations

# Visibilidade padrão das instâncias APM (mesmo valor usado pelo seed de referência).
VISIBILITY_PUBLIC = "PUBLIC"

# Tipo da Checklist (campo `type` da view Checklist).
CHECKLIST_TYPE_OEC = "OEC Route"

# Label comum aplicada às instâncias do seed.
LABEL_OEC = "OEC"

/**
 * API pública das FEATURES (DEV 3/DEV 4) que o composition root (`App.tsx`) monta no shell.
 * As visões são os pontos de entrada; tudo o mais é detalhe interno de cada feature.
 */

export { DashboardView } from './dashboard/dashboard';
export { ChecklistListView } from './checklist-list/checklist-list-view';
